COSC 301 Project:

These files include the data cleaning, and analysis questions we aimed to answer during our investigation.

To run, you will need the games.json file, which i didn't include in the repo due to file size. After running the data_cleaning notebook, the date is stored inside of SQL tables locally (sqlite3) and then you can run the analysis notebook which will export the findings as CSVs
